export default function TestComp(VPLNode){

    this.id = VPLNode.id;
    this.anchorPoint = {x:null, y:null};
    this.bBox = {x:-1,y:-1,w:-1,h:-1};
    this.anchored = false;
    

    this.show = (p5) => {

    };

    this.update = (inputs) => {

    };

}